const CustomAPIError = require('./custom-error')
const BadRequestError = require('./custom-error')
const UnauthenticatedError = require('./custom-error')

module.exports = {
    CustomAPIError, BadRequestError,
    UnauthenticatedError
}